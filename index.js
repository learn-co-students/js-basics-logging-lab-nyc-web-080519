const driver = 'bob';

console.log('this code was called')

console.log(driver)
console.log(driver, 'is the driver variable name')
console.warn(driver, 'is the driver variable name')
console.error(driver, 'is the driver variable name')